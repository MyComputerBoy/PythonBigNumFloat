# PythonBigNumFloat
Floating point format with BigNum for arbitrary resolution

So instead of using a list with bools, I'll use the BigNum behaviour in modern Python to more easily implement IEEE 754 like floating point formatting.
